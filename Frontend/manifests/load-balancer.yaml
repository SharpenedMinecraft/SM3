apiVersion: v1
kind: Service
metadata:
  name: frontend-loadbalancer
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: http
  - name: minecraft
    port: 25565
    protocol: TCP
    targetPort: minecraft
  selector:
    app: frontend
  type: LoadBalancer
